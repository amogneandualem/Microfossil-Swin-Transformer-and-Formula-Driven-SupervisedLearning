# Initialize Git LFS (Large File Storage)
git lfs install

# Tell Git to track your specific large file
git lfs track "swin_final_results_advanced/best_model.pth"

# Add the configuration files and the model
git add .gitattributes
git add .gitignore
git add swin_final_results_advanced/best_model.pth

# Commit and Push
git commit -m "Fix: Properly uploading 332MB model file via LFS"
git push
